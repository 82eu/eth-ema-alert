alert:
  cooldown_seconds: 600
  check_interval: 30
  use_email: true

ema_alert:
  ema_short: 180
  ema_long: 250
  enabled_timeframes:
  - 30m
  - 1h

email:
  # 邮箱配置（推荐使用 Render 环境变量覆盖，不写在文件里）
  # 环境变量: ALERT_FROM_EMAIL, ALERT_TO_EMAIL, ALERT_EMAIL_PASSWORD, ALERT_SMTP_SERVER, ALERT_SMTP_PORT
  from_email: ""
  to_email: ""
  password: ""
  smtp_server: smtp.qq.com
  smtp_port: 465
  use_ssl: true
  username: ""

price_ranges: []
